# --- Database Configuration (for your local PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_local_db_password
DB_NAME=egcs_db

# --- Application Port ---
# The port your backend server will run on locally.
PORT=3012

# --- JWT Authentication ---
# Use a strong, random string here. You can generate one online.
JWT_SECRET=a_very_secure_and_random_string_for_jwt_secret

# --- CORS Origin ---
# The URL of your local frontend development server .
CORS_ORIGIN=http://localhost:5173

# --- Node Environment ---
# Should be 'development' for local work and 'production' for deployment.
NODE_ENV=development